## Repositório de Igor para Cadeira de Paradigma de Linguagem de Programação UNIFAVIP

Para rodar o projeto, use o comando `poetry run start`

## Poetry

Para instalar o poetry, use o comando `brew install poetry` (MacOS)

## Dependências

Para instalar as dependências, use o comando `poetry install`
